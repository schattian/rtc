local rareName = 'rareColumnName';
local regularName = 'columnName';

{
  basic: {
    name: regularName,
    type: "string",
  },

  rare: {
    name: rareName,
    type: "string",
  },
  
  zero:{},
}
