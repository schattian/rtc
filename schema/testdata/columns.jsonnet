local basicName = 'basicColumnName';
local rareName = 'rareColumnName';

{
  basic: {
    name: basicName,
    type: "string",
  },

  rare: {
    name: rareName,
    type: "int",
  },
  
  zero:{},
}
